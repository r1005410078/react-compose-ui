# @compose-ui/commands

无 React、无 DOM、零运行时依赖的命令与键位包。

承载键位的**类型、归一化、序列化、事件匹配与平台格式化**五项能力，以及动作 id 到键位列表
的泛型映射。这五项属于同一件事：分散在不同包时，任何一个包都无法独立判定「一次按键命中了
哪个动作」或「两个动作是否撞键」。

本包不认识文档协议——动作只是 `run(ctx)`，因此可以被页面编辑器、Stage 与将来的 CAD 命令行
共用。
