# 任务

## 1. 领域操作
- [x] 1.1 `translateCadEntity`：按 Component 分派（`CadLine` 动两端、`CadInsert` 只动 `position`）
- [x] 1.2 单测：块实例位移不改块定义；未知 Component 原样返回

## 2. 命令协议
- [x] 2.1 命令步骤支持「带一条命令继续」，供 COPY 连续放置使用
- [x] 2.2 单测：连续步骤各自产出命令且会话不结束

## 3. MOVE / COPY
- [x] 3.1 `MOVE`/`M`：选择对象 → 基点 → 第二点，一个事务
- [x] 3.2 `COPY`/`CO`：连续取点，每点一个副本，结束才收束
- [x] 3.3 先选后执行：已有选择集时直接进入取基点
- [x] 3.4 单测：喂输入序列断言产出的命令与提示

## 4. 拖动手势
- [x] 4.1 `cad.move` 插件，优先级 25，claim 条件是命中**已选中**的图元
- [x] 4.2 拖动派发与 `MOVE` 相同的命令，撤销一步回到拖动之前
- [x] 4.3 拖动中显示预览；位移走点求解管线，吸附生效
- [x] 4.4 单测：命中未选中图元不 claim；命令取点期间不 claim

## 5. 端到端与验证
- [x] 5.1 e2e：框选两条线 → `M↵` → 基点 → 第二点 → 位置变化，一次撤销回到原位
- [x] 5.2 e2e：拖动已选中图元移动它，**在非 100% 缩放下**验证吸附
- [x] 5.3 五道门
