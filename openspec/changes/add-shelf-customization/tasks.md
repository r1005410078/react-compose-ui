## 1. 工具栏货架

- [x] 1.1 工具栏改为按目录 id 列表渲染（含 `'separator'`）；目录来自动作目录、命令描述符与
      `toolbarItems` prop；未知 id 跳过不报错；「选择」固定首位；复合项整体上下架；
      「设计 / 动画」与保存不在货架上
- [x] 1.2 定义加可选 `toolbar`；**两条**内建默认货架——页面（选择、指示器 | 吸附、网格 ▾ |
      容器、文字 | `RECTANGLE`、`ARROW`）与绘图（选择、指示器 | 吸附、正交、极轴 ▾、网格 ▾ |
      八条绘图命令 | 文字）
- [x] 1.3 偏好 `workspace.toolbars[id]` 与 `workspace.palettes[id]`；改过货架点亮修改点；重置与
      另存为一并处理
- [x] 1.4 右键：从工具栏移除、在此处插入分隔、自定义工具栏…。右键落在某一格上就把那一格交给
      菜单（payload 是**下标**不是 id——分隔线可以有多条，id 认不出是哪一条），落在空白处只给
      「自定义工具栏…」：空白处没有可移除的目标，列一个按下去什么都不做的项比不列更糟。
      两个回调与 `shelf` 一样走 Context——中间那一层属于宿主。
- [x] 1.5 自定义对话框：两列拖动（货架 / 未放入）、插分隔、重置为默认；写 `toolbars[id]`
- [ ] 1.6 Testing Library：移除「容器」后按钮消失而 `stage.drawContainerTool` 快捷键仍可用；重置后
      回来；未知 id 不渲染；「选择」不可移除；极轴 ▾ 作为整体移动
- [x] 1.7 目录准入校验：逐项**声明**第二条入口并验证它真的在（不是一张白名单——白名单在加进
      新一格时永远绿）。这条规则当场抓到两处真缺口：**画布设置**只在网格 ▾ 菜单里、**变换
      指示器**一条动作 / 键位 / 命令词都没有。补了 `stage.canvasSettings` 与
      `stage.toggleTransformGizmo` 两条动作（默认都不绑键）；不补的话这两格就该退出目录。

## 2. 物料面板自定义

- [ ] 2.1 右键瓦片：基础组件可隐藏；文件夹来源的瓦片只有「只看这一组」与「在资源里打开此文件夹」；
      「自定义物料面板…」
- [ ] 2.2 自定义对话框：面板标题、来源拖动排序、文件夹来源两个开关、基础组件勾选、添加来源
      （复用资源浏览器的文件夹选择）、搜索框开关、重置为默认；写 `palettes[id]`
- [x] 2.3 Vitest：货架编辑的纯函数（增删排序、隐藏 Preset、添加来源）
- [x] 2.4 `paletteHidden` 判据改写：拆成「因为工具栏已提供入口」与「物料自身的理由」两类，
      前者按当前工作区的工具栏货架算；`resolveComponentShelf` 的 `presets` 由调用方按当前货架
      算好再传，包内不认识工具栏
- [x] 2.5 Vitest：页面工作区里圆的瓦片出现、绘图里不出现；导线的瓦片两边都不出现

## 3. 端到端

- [ ] 3.1 从绘图的工具栏移除「容器」：按钮消失，`Shift+C` 快捷键与命令行里的动作仍可用；重置后回来
- [ ] 3.2 宿主经 `toolbarItems` 上架 `MIRROR`：按钮出现，点击启动与命令行敲 `MIRROR` 相同的会话
- [ ] 3.3 对话框把绘图命令组拖到吸附组之前：工具栏按新顺序渲染，切走再切回仍在；窄窗口下尾部进「更多」
- [ ] 3.4 物料面板去掉「布局容器」后瓦片消失，资源拖入仍可用；「只看这一组」只剩那一组；添加
      `Symbols / 变电站` 来源后出现新的一段
- [ ] 3.5 「另存为」之后新工作区带着改过的两条货架；删除来源工作区不影响它
- [x] 3.6 两个内建的默认货架不同：页面的工具栏没有 `CIRCLE` / 正交 / 极轴而绘图的有；绘图没有
      容器而页面有
- [x] 3.7 页面工作区里 `CIRCLE` 仍可用：敲命令、按 `C`、命令面板三条路都启动同一条会话
- [x] 3.8 页面工作区的基础组件段里出现「圆」瓦片，切到绘图后消失
- [x] 3.9 黄金图：两张工作区图**无需重录**（工具栏那点面积在 1% 容差内），
      `component-library-dock.png` 重录了——页面工作区里圆的瓦片按新判据出现。
- [x] 3.11 修 `overflow: hidden` 裁掉工具栏下拉菜单：网格 ▾ / 极轴 ▾ /「更多」都是挂在栏内锚点上
      的绝对定位元素，从 48px 的行底伸出去，被一并裁掉后只露出第一项。改成 `overflow-x: clip` +
      `overflow-y: visible`——横轴那一档是 JS 没跑起来时的兜底，要留；`hidden` 会把另一轴的
      `visible` 强制解析成 `auto`，因此只有 `clip` 写得成这条规则。判别性断言用**命中测试**
      （`elementFromPoint` 落在菜单里）：`boundingBox()` 与 `toBeVisible()` 在被裁时照样绿，
      已按「先证明它会红」验过。
- [x] 3.10 既有用例适配：绘图命令与正交 / 极轴的按钮不再在页面货架上，9 个 spec 里的相关用例
      改为先切到绘图工作区（新增 `useDrawingWorkspace` 助手）。顺带修掉一处**既有的测试脆弱性**
      ——`editor-workspace` 的拖拽用例读了过期的瓦片坐标，多一块瓦片就把面板推走了，补
      `scrollIntoViewIfNeeded`。

## 4. 文档与验证

- [x] 4.1 `README.md` 补工具栏货架与自定义入口；`AGENTS.md` 当前阶段补三条判据：货架是入口的
      货架不是能力的清单、货架不得成为唯一入口（含它抓到的两处缺口）、`paletteHidden` 按当前
      工作区的货架判定。物料面板的自定义那一半随 2.1 / 2.2 一起补。
- [ ] 4.2 `bun run lint`、`typecheck`、`test`、`build`、`test:e2e` 全绿
      —— 截至右键入口落地：前四项全绿；`test:e2e` 那一轮 262/2，两条红单独跑全绿，仍是
      `add-builtin-workspaces` 4.2 记的那个启动闪烁在游走（每轮集合都不同），与本变更无关。
      在此之前的一轮是 264 passed / 0 failed。此前记在
      `add-builtin-workspaces` 4.2 里的那个启动闪烁本轮没有复现，但它是概率性的，不能据此
      认为已经消失。剩余任务（右键菜单、物料面板自定义对话框）落地后需再跑一轮。
