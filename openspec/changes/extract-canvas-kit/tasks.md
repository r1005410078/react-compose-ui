# 任务

## 1. 无 React 部分下沉到 core
- [ ] 1.1 视口模型迁入 `core`：世界↔屏幕、按屏幕位移平移、绕锚点缩放（钳制范围由调用方给）
- [ ] 1.2 轴点阵与标尺刻度迁入 `core`，入参改为普通数字，不再认识任何视口类型
- [ ] 1.3 视口适配几何迁入 `core`
- [ ] 1.4 `stage-engine` 改为转导，既有导出名不变
- [ ] 1.5 **Stage 全绿**：点阵、标尺、视口适配的既有用例一条不改

## 2. 新建 canvas-kit
- [ ] 2.1 建包，依赖 `core` 与 `ui-context`，React 为 peer；写 `@packageDocumentation` 边界
- [ ] 2.2 依赖边界用例：不依赖 `stage`、`stage-engine`、`cad`、`editor`；源码不出现文档或选择集类型
- [ ] 2.3 滚轮导航 Hook 迁入（非 passive 原生监听、latest-ref、指数换算三条注释一并搬）
- [ ] 2.4 图面尺寸 Hook 迁入
- [ ] 2.5 标尺画笔与 React 外壳迁入，命名与类名去 Stage 化，调色板改用包内语义 token
- [ ] 2.6 自定义滚动条迁入
- [ ] 2.7 `stage` 改为消费 canvas-kit，辅助线拖拽仍留在 Stage 侧
- [ ] 2.8 **Stage 全绿，含黄金图零漂移**

## 3. CAD 网格
- [ ] 3.1 CAD 视口改用共享模型，删除 `cadZoomViewport` 等重复实现
- [ ] 3.2 CAD 滚轮改用共享 Hook，删除 `cad-surface` 内的原生监听
- [ ] 3.3 网格改用共享点阵并按 CSS 多层 gradient 绘制，删除 SVG 网格线节点
- [ ] 3.4 组件测试：缩到间距不足时抽稀为二次幂子集而不是消失；抽稀不改变实际吸附步长
- [ ] 3.5 组件测试：同一世界坐标的刻度线与网格线覆盖同一条 1 CSS px 带

## 4. CAD 标尺
- [ ] 4.1 CAD 画布挂上/左标尺与原点角，宿主可关闭
- [ ] 4.2 游标线跟随指示点；选择集存在时显示包围盒区间与尺寸
- [ ] 4.3 组件测试：刻度随平移缩放对齐；指针离开后游标消失

## 5. 文档与验证
- [ ] 5.1 AGENTS.md 架构边界补 `@compose-ui/canvas-kit`，并写明三类不得进入的内容
- [ ] 5.2 e2e：CAD 缩小后网格仍可见；标尺刻度与网格线对齐
- [ ] 5.3 五道门
