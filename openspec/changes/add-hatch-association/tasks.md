## 1. 协议（core）

- [ ] 1.1 `Hatch.boundaryIds?: string[]`：类型、校验（空数组非法）、缺席即不跟随
- [ ] 1.2 单测：缺席、空数组非法、既有文档逐字不变

## 2. 选点（core）

- [ ] 2.1 `core/curve-region.ts` 新增最大内切圆圆心纯函数；弧边按固定份数采样
- [ ] 2.2 带洞的面：洞的边界一并算，结果不得落在洞里
- [ ] 2.3 退化的面返回缺席
- [ ] 2.4 单测：矩形取中心、L 形不取包围盒中心、洞把圆心推开、退化返回缺席

## 3. 落地时记下清单（stage）

- [ ] 3.1 `drafting-entity.ts` 把已经算出来的边界 id 写进 `Hatch.boundaryIds`
      （MUST 取层序用的那一份，不再求一次）
- [ ] 3.2 改既有对象那一支不写 `Hatch`
- [ ] 3.3 单测：新建那一支写下清单；改填充那一支不产出 `Hatch`

## 4. 跟随（stage）

- [ ] 4.1 事务后置钩子：`targetIds` ∩ `boundaryIds`，交集为空则一次都不跑
- [ ] 4.2 重求 → 三档分流：清单相同即跟随、不同即过期、求不出即失效
- [ ] 4.3 跟随成功时重取锚点到最大内切圆圆心，并更新 `boundaryIds`
- [ ] 4.4 写入并进**同一个事务**（撤销一步全回去）
- [ ] 4.5 手势期不跑（判据是文档变没变，不是手在不在动）
- [ ] 4.6 单测：四种分流、过滤器为空时零次求解、清单更新、同一事务

## 5. 端口与 Inspector（component-registry / materials）

- [ ] 5.1 `ComposeHatchEditPort` 加 `detach`；`isStale` 升级成三档状态查询
- [ ] 5.2 Inspector：三档文案、「断开关联」按钮、「取点」改口成「锚点」
- [ ] 5.3 端口缺席时不画那几颗按钮
- [ ] 5.4 组件测试：三档各一条、断开之后 `Hatch` 消失而几何与填充色保留、端口缺席

## 6. 端到端

- [ ] 6.1 拖顶点 → 填充跟上，撤销一步边界与填充一起回去
- [ ] 6.2 挪一个不相干的符号 → 填充一个字节不变
- [ ] 6.3 圆整个挪出去 → 几何不变且标过期；按「重新生成」之后跟上且清单更新
- [ ] 6.4 剪开边界 → 标失效且 `Hatch` 仍在；补上那一段后「重新生成」即回来
- [ ] 6.5 断开关联 → 再动边界它不动
- [ ] 6.6 拖动过程中填充不动，松手才跟上
- [ ] 6.7 已落地（无 `boundaryIds`）的填充不跟随，第一次「重新生成」补上清单
- [ ] 6.8 用例 MUST 在非 100% 缩放下断言

## 7. 收尾

- [ ] 7.1 `bun run lint && bun run typecheck && bun run test && bun run build`
- [ ] 7.2 `bun run test:e2e`
- [ ] 7.3 按落地情况更新 `docs/mockups/drafting-hatch-assoc.html` 并重发 artifact（同一个 URL）
- [ ] 7.4 按需同步 `AGENTS.md`（三档、清单的两个用途、锚点语义的改变、不做分组）
