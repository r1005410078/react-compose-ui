## 1. 应用顶栏与标志

- [x] 1.1 把 `WorkspaceDocumentTabs` 拆成两个组件：`EditorTopBar`（标志 ▾ ｜ 工作区 ｜ 布局开关）
      与 `DocumentTabs`（只剩标签本身）；顶栏仍渲染在 Dockview 之外、`compose-editor__workspace` 的第一行
- [x] 1.2 新增 `editor-brand-menu.tsx`：标志按钮（`aria-haspopup="menu"`）+ 菜单（设置、命令面板），
      `Escape` / 点击外部关闭并把焦点还给触发按钮，方向键在项间移动
- [x] 1.3 画标志 SVG：两块实心圆角方块 + 一段 accent 正交导线；顶栏 19px、favicon 16px 各验一次可读
- [x] 1.4 删除顶栏右端的设置齿轮；设置改由菜单项触发，`editor.settings` 动作与键位保持不动
- [x] 1.5 去掉顶栏段与段之间的竖线（`.compose-editor__tabs-zone + .compose-editor__tabs-zone` 的 border）

## 2. 文档标签条下沉到画布列头

- [x] 2.1 `CanvasPanel` 在工具栏之上渲染 `DocumentTabs`，高度 30px，位于画布面板内容的第一行
- [x] 2.2 确认它与左右两栏的 Dockview 组头顶边同 y（画布组组头本来就隐藏，无需对齐代码）
- [x] 2.3 标签加文档类型图标（页面 / 组件 / 资源按扩展名分档），保留未保存圆点与关闭按钮
- [x] 2.4 关闭按钮改成只在 hover / focus 显出自己的底
- [x] 2.5 标签条溢出时横向滚动，不折成 `▾`（本期结论，见 design 待解决问题）
- [x] 2.6 未启用页面系统时的固定「画布」格仍在第一位且不可关闭

## 3. 统一的选中画法

- [x] 3.1 文档标签：改成圆角标签，活动 = `surface-hover` 底 + `text-strong`，去掉整格底与竖线分隔
- [x] 3.2 Dockview 面板头标签：同一套画法（`.dv-tab` 的活动 / 静息样式改为圆角底）
- [x] 3.3 检查全仓库没有第二处用下划线 / accent 条表示活动的 chrome
- [x] 3.4 hover 只改文字颜色，确认不上底
- [x] 3.5 画布工具栏按下态保持 `surface-selected` 不动，并在用例里钉住它与顶栏灰底不同色

## 4. 工作区改成一组 pill

- [x] 4.1 去掉外框盒、竖线与每格图标；活动格改成圆角底
- [x] 4.2 `▾` 仍挂在活动格上、仍在 `radiogroup` 之外（`order` 方案不变）
- [x] 4.3 末尾加 `＋`，复用管理菜单「另存为」的同一条流程与同一份对话框
- [x] 4.4 确认 `＋` 与 `▾` 都不在方向键序列里

## 5. 用例与验证

- [x] 5.1 组件测试：标志菜单的展开 / 关闭 / 焦点归还；`＋` 与「另存为」走同一条流程
- [x] 5.2 端到端：文档标签条与左右面板头顶边同 y、同高 30px
- [x] 5.3 端到端：画布内容顶边距编辑器顶边 96px
- [x] 5.4 端到端断**计算后的颜色**——活动标签 / 活动工作区格的背景，以及工具栏按下态仍是蓝底；
      先验红再验绿（本仓库有过样式被无层规则静默压掉的先例）
- [x] 5.5 端到端：顶栏上没有文档标签、没有保存按钮、没有齿轮、没有模式切换器
- [x] 5.6 修既有用例中依赖「标签条在顶栏 / 齿轮在右端」的定位；**行为断言一个字不动**
- [x] 5.7 `bun run lint` / `typecheck` / `test` / `build` / `test:e2e` 全绿，黄金图按需重录
