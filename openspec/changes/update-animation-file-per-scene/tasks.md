# 任务：动画文件默认按场景独立

## 1. 创建策略

- [x] 1.1 删除 `compose-editor.tsx` 中 `activePageAnimationReference` 的页面级引用复用
      逻辑；空态创建（`handleCreateAnimationFromEmptyState`）改为总是为作用域场景创建
      独立文件，命名传「页面名-场景名」
- [x] 1.2 确认 `createPageAnimationFile` 的同名冲突追加序号机制覆盖新命名（复用既有实现，
      仅调用方传入的 displayName 变化）
- [x] 1.3 更新空态创建的单测：场景 A 已绑定时，场景 B 创建得到新文件而不是写进 A 的分区

## 2. 页面配置面板

- [x] 2.1 把动画分组改为按 `rootIds` 逐场景渲染绑定行（重构 `PageAnimationScopePanel`
      或新建逐场景列表组件）：行标签为场景名，标注激活场景与当前动画作用域场景
- [x] 2.2 每行支持绑定/更换/解绑/快捷创建；已绑定行保留播放控制变量绑定入口，
      候选仍取页面 setup 作用域成员
- [x] 2.3 绑定/解绑走 `animation.source.set` 可撤销命令（既有），文件创建保持资源写入；
      共享文件页面两行显示同一文件、可独立解绑
- [x] 2.4 新增/调整编辑器 i18n 文案（行标注、创建入口、失败提示）
- [x] 2.5 面板单测：逐场景行渲染顺序、激活/作用域标注、独立解绑互不影响

## 3. 保存回写

- [x] 3.1 `use-page-workspace.ts` 保存循环补充单份文件写入失败/冲突的显式提示，
      失败不阻塞页面本体与其他文件的保存（聚合逻辑已按 assetKey 分组，无结构变更）
- [x] 3.2 单测：A、B 绑定不同文件时各写一次；共享一份时只写一次；B 文件写失败时
      A 与页面本体仍保存成功且有提示

## 4. 端到端与验证

- [x] 4.1 E2E：两块场景各自创建动画 → 各得独立文件 → 保存重开后动画都在
- [x] 4.2 既有共享文件页面照常加载、面板如实显示两行同一文件（演示应用无法在 UI 中
      制造共享状态，改以组件测试与 page-workspace 单测覆盖：共享文件合并回写只写一次、
      面板两行同文件可独立解绑）
- [x] 4.3 运行 `bun run lint && bun run typecheck && bun run test && bun run build`
      与 `bun run test:e2e`
- [x] 4.4 同步 `AGENTS.md`「动画按场景独立」段落与 README 完成度描述（一页一份文件的
      表述改为默认一场景一份、共享文件兼容）
