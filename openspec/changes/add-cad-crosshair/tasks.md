# 任务

## 0. 指示点
- [ ] 0.1 抽出具名指示点会话状态：存屏幕位置，按点求解管线解算出世界落点与解算来源
- [ ] 0.2 十字光标、橡皮筋终点、坐标读数、捕捉标记改为消费同一个指示点
- [ ] 0.3 单测：解算结果与来源随捕捉/正交/网格变化；平移中世界落点保持不变
- [ ] 0.4 组件测试：框选拖拽与中键平移期间指示点继续更新，不被手势会话接管

## 1. 光标形态
- [ ] 1.1 从 `prompt.accepts` 求光标形态：idle / point / selection / 无
- [ ] 1.2 图面渲染十字线与拾取框，`cursor: none` 隐藏系统指针
- [ ] 1.3 十字线按一对轴向量绘制，长度取 `crosshairSize` 百分比
- [ ] 1.4 拾取框半边长取 `pickRadius`，随缩放保持屏幕尺寸
- [ ] 1.5 十字线在拾取框处断开，框内不留线段

## 2. 属性与默认值
- [ ] 2.1 `crosshairSize` prop（1–100，默认 100）并写明与 `CURSORSIZE` 的对应
- [ ] 2.2 `showCrosshair` prop（默认 true）；关闭时不隐藏系统光标
- [ ] 2.3 按 `pointerType` 分流：mouse/pen 绘制并隐藏系统光标，touch 两者都不做
- [ ] 2.4 十字光标位置以命令式更新，不走 React 渲染路径

## 3. 测试
- [ ] 3.1 组件测试：三种形态各自出现与消失；指针离开图面后光标消失
- [ ] 3.2 组件测试：拾取框半边长等于 `pickRadius`，缩放后不变；框内无十字线
- [ ] 3.3 组件测试：指针移到图元上时拾取框外观不变，命中只由悬停高亮表达
- [ ] 3.6 组件测试：touch 指针不绘制十字光标且不隐藏系统光标；`showCrosshair` 关闭时同理
- [ ] 3.4 e2e：敲 `L` 后拾取框消失、敲 `E` 后十字线消失
- [ ] 3.5 五道门
