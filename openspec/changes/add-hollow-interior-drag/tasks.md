## 1. 插件

- [x] 1.1 `hollow-move-fallback` 插件：单击、无 Shift、选中恰好一个空心曲线、按下点落在它盒内、
      且这一下本来会变成框选时，复用 `claimStageMove` 开一次移动会话
- [x] 1.2 只接管拖：包一层会话，按**指针有没有离开过按下点**判断；没动则 `cancel` + 清空选区
- [x] 1.3 优先级表加 825 一项，登记处与导出跟上
- [x] 1.4 单元测试：拖动即移动且不拉框选、一步没动仍是单击、没选中不接管、Shift 不接管、
      填过色不走这条路、盒外面照旧框选、优先级夹在两者之间

## 2. 端到端

- [x] 2.1 复现原症状的用例：选中空心矩形→从盒中间拖 60px→对象跟着走、框选没出现、选区还在
- [x] 2.2 另一半：盒内部一步没动的那一下仍是单击（清空选区且对象没动）

## 3. 文档

- [x] 3.1 `AGENTS.md` 补这条分界线（没选中不拦、选中之后只接管拖）

## 4. 验证

- [x] 4.1 `bun run lint` / `typecheck` / `test` / `build`
- [x] 4.2 `bun run test:e2e`
- [x] 4.3 `openspec validate add-hollow-interior-drag --strict`
