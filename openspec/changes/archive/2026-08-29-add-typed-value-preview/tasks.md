## 1. 候选落点

- [x] 1.1 `use-stage-drafting`：活动字段缓冲可解析为数字时，在 `resolvedPointer` 之上再做一次
      字段覆盖，得到候选落点
- [x] 1.2 预览查询、十字光标、橡皮筋终点、捕捉标记与标注几何全部改读候选落点
- [x] 1.3 缓冲不可解析时候选落点恒等于 `resolvedPointer`

## 2. 连线

- [x] 2.1 候选落点与解算落点不等时，标注里多一条从候选落点到光标的极淡连线
- [x] 2.2 样式带字面量兜底

## 3. 测试

- [x] 3.1 组件测试：`LINE` 取过一点后键入 `100`，预览线终点到起点的距离是 100
- [x] 3.2 组件测试：键入 `@100,` 时预览仍跟着光标
- [x] 3.3 组件测试：键入过程中会话不推进（提示不变）
- [x] 3.4 端到端：键入长度时图面上的线立刻变长，回车前不落地

## 4. 验证

- [x] 4.1 `bun run lint` / `typecheck` / `test` / `build`
- [x] 4.2 `bun run test:e2e`
