## 1. core 数学

- [x] 1.1 `ComposeAngleConstraint`（`off` / `ortho` / `polar`）与增量角、容差进入
      `ComposePointContext`
- [x] 1.2 极轴投影：按增量角求最近射线，落点到射线的距离在容差内才投影
- [x] 1.3 网格改排在角度约束**之前**
- [x] 1.4 上报「命中了哪条射线」，供呈现层画追踪射线
- [x] 1.5 用例：容差外不吸、容差内落在射线上、正交仍无条件投影、参考点不在格点时正交不被
      网格挪走、键入坐标与对象捕捉仍然短路

## 2. Stage 状态与键盘

- [x] 2.1 `ortho: boolean` 换成三态；默认 `polar`
- [x] 2.2 `angleConstraint` / `onAngleConstraintChange` 可选受控 prop
- [x] 2.3 `F8` 切正交、`F10` 切极轴（按下已生效的那一个即关闭）
- [x] 2.4 命令行状态标记跟着三态走
- [x] 2.5 增量角作为 Stage prop（宿主给，缺省 45）

## 3. 追踪射线

- [x] 3.1 命中时在覆盖层画一条从参考点穿过落点的射线
- [x] 3.2 样式属于标注族，带字面量兜底

## 4. 工具栏

- [x] 4.1 吸附组新增正交与极轴两个按钮，互斥、按下态可见
- [x] 4.2 极轴按钮带增量角下拉（AutoCAD 的八个约数），与网格大小菜单同构
- [x] 4.3 controller 持有状态并传给 Stage
- [x] 4.4 文案与图标

## 5. 测试

- [x] 5.1 组件测试：默认极轴，接近 45° 的取点落在射线上，远离时自由
- [x] 5.2 组件测试：按正交按钮之后极轴关闭，且斜向取点被钳到轴上
- [x] 5.3 组件测试：改增量角为 90 之后 45° 方向不再被吸
- [x] 5.4 端到端：工具栏切换可见、追踪射线出现、落点与它一致

## 6. 既有用例适配

- [x] 6.1 逐条跑过端到端，依赖自由角度的显式关掉角度约束
- [x] 6.2 在 spec 文件的文档注释里写明这条纪律

## 7. 验证

- [x] 7.1 `bun run lint` / `typecheck` / `test` / `build`
- [x] 7.2 `bun run test:e2e`
