## REMOVED Requirements

### Requirement: Stage 页面文档加载注入

**原因**：该端口只为 Page Slot 存在——把页面文档加载能力注入 Registry 渲染上下文，是为了让
画布上的槽位实时渲染被引用页面。页面嵌套已删除，端口失去唯一消费方，留着它就是横穿五个包
的死管线。

**迁移**：无。Stage 渲染的是正在编辑的文档，不需要加载其他页面；跳转只在预览宿主里生效。
