## ADDED Requirements

### Requirement: 标尺压在图面上

Stage 的标尺 MUST NOT 带底色与边框，只保留刻度与数字，叠在图面之上。判据是分割数量：标尺
带框时它是画面上两条额外的边，与边缘组一起把 chrome 切成八条竖线；去掉之后它是坐标参照而
不是框。刻度、游标线与吸附行为 MUST 不变。

#### Scenario: 标尺不占框

- **WHEN** 编辑器渲染画布
- **THEN** 标尺区域的背景与图面相同，只有刻度线与数字可见
- **AND** 标尺的刻度与游标线行为与此前一致
