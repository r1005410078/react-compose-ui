## 0. 前置

- [x] 0.1 核心面板标题「工作区」（`messages.workspaceCore`）改名「编辑区」，两种语言——实际做法：承载它的外层核心面板随三层实例一起删除，这个标题不再存在

## 1. 单一 Dockview

- [x] 1.1 三层实例收成一个：底部为原生边缘组，左右为普通组（场景图与工具组上下分栏、属性）
      （60%/40%、最小高度 160 / 120），画布组在中央；`locked` 与 `disableDnd` 原样保留
- [x] 1.2 边缘面板按文档类型记忆展开状态改到单一实例上，行为不变
- [x] 1.3 Strict Mode 组件测试：每组每面板各一个实例；landmark 只剩一份 `aria-label`

## 2. 文档标签条

- [x] 2.1 文档（页面 / 组件 / 资源文件）从 Dockview panel 改为会话，中央画布组只承载画布；
      资源文件的未保存 Monaco 草稿由会话保住
- [x] 2.2 通栏 `tablist`：方向键在文档间移动、`Delete` 不关闭、未保存圆点、关闭确认沿用既有
      dirty 流程；样式沿用今天的标签
- [x] 2.3 设置齿轮挪到标签条右端；打开设置不重建 Dockview
- [x] 2.4 Testing Library：tablist 键盘、齿轮可聚焦、切走再切回资源文件草稿仍在

## 3. 边缘组与面板头

- [x] 3.1 画布组 `hideHeader`；场景组与属性组头上的折叠按钮；收起后 8px 把手；面板头高度 30px
- [x] 3.2 收起再展开恢复尺寸且内容不重挂载（组件测试）

## 4. 标尺与工具栏

- [x] 4.1 标尺去掉底色与边框，只留刻度与数字压在图面上（`stage` 样式）；刻度与游标线用例不变
- [x] 4.2 工具栏溢出：放不下的按钮按顺序从尾部收进「更多」菜单；「设计 / 动画」与保存挪到文档标签条右端（默认三栏下工具栏才放得下），按钮收成 30px

## 5. 端到端与黄金图

- [x] 5.1 `editor-workspace.spec.ts`：只有一个 Dockview；文档标签条宽度等于编辑器宽度；画布组头
      没有标签；左右没有图标轨；窄窗口下工具栏尾部进「更多」
- [x] 5.2 十二张黄金图重录

## 6. 文档与验证

- [x] 6.1 `README.md` Stage / Inspector 一节补 chrome 描述
- [x] 6.2 `bun run lint`、`typecheck`、`test`、`build`、`test:e2e` 全绿
