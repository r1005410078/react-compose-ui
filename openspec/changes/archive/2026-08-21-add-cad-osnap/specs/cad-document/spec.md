## ADDED Requirements

### Requirement: CAD 对象捕捉

系统 MUST 支持在指定点时捕捉到既有几何的特征点：端点、中点与交点。捕捉半径 MUST 以**屏幕
像素**表达，由宿主换算为世界单位——视图缩小时同样的屏幕半径覆盖更大的世界范围。

候选 MUST 先按模式优先级排序（端点 > 中点 > 交点），同级再按到目标点的距离。半径内没有候选时
MUST 返回空，而不是给出最近的任意点。

捕捉 MUST 是点求解管线中**优先级最高**的一级：命中时结果就是特征点，MUST NOT 再被正交或
网格改写。

#### Scenario: 端点优先于中点

- **WHEN** 捕捉半径内同时存在某条线的端点与另一条线的中点
- **THEN** 结果是端点
- **AND** 同为端点时取更近的那个

#### Scenario: 捕捉到两条线的交点

- **WHEN** 捕捉半径内存在两条相交直线的交点且没有更高优先级的候选
- **THEN** 结果是该交点
- **AND** 平行或不在线段范围内相交时不产生交点候选

#### Scenario: 半径外不捕捉

- **WHEN** 目标点与任何特征点的距离都超过捕捉半径
- **THEN** 不返回候选
- **AND** 点按原有管线（正交、网格）求解

#### Scenario: 捕捉压过正交与网格

- **WHEN** 捕捉命中且正交与网格都开启
- **THEN** 结果是特征点本身，不被钳到轴向也不被网格取整

#### Scenario: 隐藏图层不参与捕捉

- **WHEN** 某条线所在图层不可见
- **THEN** 它的端点、中点与交点都不作为候选

### Requirement: CAD 捕捉标记

命令等待取点时，系统 MUST 在当前捕捉到的特征点上渲染标记，并按模式区分形状：端点方框、
中点三角、交点叉号。没有命中时 MUST 不渲染任何标记。

对象捕捉 MUST 可切换；关闭时 MUST 不产生候选也不渲染标记。

#### Scenario: 标记随指针移动更新

- **WHEN** 指针在既有直线的端点附近移动
- **THEN** 端点上出现方框标记
- **AND** 指针移开捕捉半径后标记消失

#### Scenario: 关闭对象捕捉

- **WHEN** 用户关闭对象捕捉
- **THEN** 不再出现任何捕捉标记
- **AND** 取点按正交与网格求解
