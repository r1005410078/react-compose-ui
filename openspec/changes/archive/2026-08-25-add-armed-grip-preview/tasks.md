# Tasks

## 1. 判别性用例先验红

- [x] 1.1 e2e：点亮一个端点夹点，把光标移到别处，断言可编辑路径的轮廓点**变了**且夹点跟到
      解算后的落点上。**先跑确认红**——今天轮廓一动不动
- [x] 1.2 同一条：断言此时文档几何还没变（属性面板读数不动）
- [x] 1.3 断言点亮期仍然点亮、仍然不画拾取框——预览不得把点亮误判成拖动
- [x] 1.4 断言预览读的是**解算后**的落点：光标停在另一条曲线端点的容差内但不在它上面时，
      预览落在那个端点上而不是裸坐标
- [x] 1.5 指针移出图面时预览退回文档几何
- [x] 1.6 回归：拖动期的预览与提交都不变

## 2. `stage`：交出解算后的落点

- [x] 2.1 `useStageDrafting` 暴露解算后的世界落点，与 `pointerScreen`、`rubberBand.end`
      与捕捉标记同源；MUST NOT 让几何编辑再解算一遍

## 3. `stage`：点亮期重算预览

- [x] 3.1 `use-stage-geometry-editing` 在会话点亮且非拖动时，按解算后的落点重算预览
- [x] 3.2 `dragging` 改成自持状态（指针手势在不在进行中），不再由 `preview !== null` 派生
- [x] 3.3 指针为空时不出预览
- [x] 3.4 「`dragging` 与预览解耦之后热夹点与拾取框不受影响」由 e2e 1.3 承担——它要两个 Hook
      真的接在一起才成立，组件级复现等于把接线重写一遍
- [x] 3.5 **实现时才发现的一条**：绘图 Hook 必须排在几何编辑之前，否则预览慢光标一帧；
      反方向的两处依赖改走 ref

## 4. 规范与文档

- [x] 4.1 `stage` 规范增量（已写）
- [x] 4.2 AGENTS.md：点亮期实时预览；**更正热夹点的理由**——它说的是「有一次编辑正在进行」，
      「停在原位」那句随实时预览作废
- [x] 4.3 路线图：新增步骤 13b（含用例夹具的坑：文档几何要在点亮**之前**读）；步骤 13 补一段，记下这条遗留与被更正的理由

## 5. 五道门

- [x] 5.1 `bun run lint`
- [x] 5.2 `bun run typecheck`
- [x] 5.3 `bun run test`
- [x] 5.4 `bun run build`
- [x] 5.5 `bun run test:e2e`
