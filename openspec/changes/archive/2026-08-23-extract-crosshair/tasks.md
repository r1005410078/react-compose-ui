# Tasks

## 1. 判别性用例先验红

- [x] 1.1 `e2e/drafting.spec.ts` 加一条：启动 `LINE` → 悬停 → 断言图面上有**四条**十字线
      （两轴各两个方向）且 `cursor` 为 `none`。**先跑确认红**——今天 Stage 画的是两条贯穿
      视口的线，而系统光标仍是默认箭头
- [x] 1.2 同一条继续：`Escape` 回到空闲后不再绘制；选中两个对象再启动 `ERASE`（等待选择的
      那一档）断言拾取框出现而十字线为零。今天这两种形态在 Stage 上都不存在
- [x] 1.3 CAD 侧**不加新用例**：既有组件测试与 e2e 一行不改即全绿，是搬运没有改变行为的证据

## 2. `canvas-kit`：共享十字光标

- [x] 2.1 新增 `crosshair/` 功能目录：受控 `<g>` 组件 + 纯解析函数 + 样式 + 测试
- [x] 2.2 解析函数是「是否在画」的唯一判据，四种不画的情形（宿主关闭 / touch / 指针不在
      图面 / 两个形态布尔都为假）都由它给出
- [x] 2.3 十字线按一对轴向量绘制，单侧长度按视口**较短边**取百分比；同时画框时在框处断开
- [x] 2.4 `data-testid` 前缀由调用方给出；线元素带属性标记，便于按数量断言形态
- [x] 2.5 样式只给笔（描边、线宽、不透明度、`fill: none`），不给摆位
- [x] 2.6 组件测试：两个布尔的四种组合、长度百分比、断开、touch、指针离开
- [x] 2.7 依赖边界用例保持不变——本包仍只依赖 `core` 与 `ui-context`

## 3. `cad-canvas`：改用共享实现并修正落点

- [x] 3.1 删除包内的 `Crosshair` 组件与 `CadCrosshair` 类型，改用共享的
- [x] 3.2 形态推导（由 `prompt.accepts` 推出两个布尔）留在本包
- [x] 3.3 **落点改成解算后的世界坐标**换算的屏幕位置，与橡皮筋终点、坐标读数、捕捉标记一致
- [x] 3.4 `.compose-cad-canvas__crosshair` 的画笔样式移交 canvas-kit；隐藏系统光标那条规则
      留在本包（它作用于整个图面区域，不是 SVG 元素自身的样式）
- [x] 3.5 `data-cad-crosshair-line` 与 `cad-pickbox` 由前缀 `cad` 复现，既有断言不改
- [x] 3.6 修正落点会让 `reach()` 夹具读到吸附后的中心：按吸附后的值取坐标，**断言本身不变**

## 4. `stage`：补齐三形态

- [x] 4.1 绘图覆盖层改用共享组件；testid 由 `stage-drafting-crosshair` 变为 `stage-crosshair`
- [x] 4.2 形态推导：等待取点画线、等待选择画框、其余不画。**空闲不画**是刻意的不对称，
      在实现处写明理由
- [x] 4.3 覆盖层的挂载条件从「等待取点」放宽到「有东西要画」，否则等待选择那一档挂不上
- [x] 4.4 绘制期间给图面容器挂上隐藏系统光标的标记，判定与绘制读同一个值
- [x] 4.5 `ComposeStageProps` 增加 `showCrosshair` / `crosshairSize` / `pickRadius`，
      与 CAD 同名同义；`pickRadius` 的 TSDoc 写明它在 Stage 上不参与命中判定
- [x] 4.6 组件测试：三形态、隐藏系统光标的标记、宿主关闭

## 5. 回归

- [x] 5.1 `bun run lint` / `typecheck` / `test` / `build` 全绿
- [x] 5.2 `bun run test:e2e` 全绿，跑**两轮**且事先确认没有残留的 preview/test-server 进程
- [x] 5.3 黄金图：Stage 的绘制工具预览截图不受影响（本刀不动绘制工具的光标）
- [x] 5.4 `AGENTS.md` 补一段共享十字光标与两条不对称（空闲形态、拾取框不判命中），
      路线图回填步骤 9 实测
