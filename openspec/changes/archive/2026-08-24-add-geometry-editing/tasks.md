# Tasks

## 1. 判别性用例先验红

- [x] 1.1 新增 `e2e/geometry-editing.spec.ts`：`?no-auto-fit` 下把缩放调离 100%，画一条水平
      曲线 → 双击 → 断言图面出现**两个**夹点、且该对象的 Resize 手柄为零。**先跑确认红**——
      今天双击曲线什么也不发生
- [x] 1.2 同一条继续：把一个端点拖到另一个高度并松手，断言两端不再等高（这正是 8b 记下的
      那笔债：轴对齐的线掰不成斜的），再撤销一步回到水平
- [x] 1.3 既有运动路径用例一行不改即全绿，是「复用没有改变宿主那条路径」的证据

## 2. `stage-engine`：夹点派生与顶点求解

- [x] 2.1 新增 `geometry-editing/` 功能目录：夹点派生 + 顶点编辑求解 + 类型 + 测试
- [x] 2.2 派生共用 `projectComposeCurveToBox`，与命中、捕捉同一个投影；与
      `curveFeaturePoints` 挨着放但**不合并**，在实现处写明理由
- [x] 2.3 逐 kind 夹点集；整圆只出圆心与中点
- [x] 2.4 弧的四个夹点各只改一个自由度；扫掠角保号、归一化到同号 `(0, 360]`、0 取 360
- [x] 2.5 Vitest：逐 kind 夹点数、整圆退化、拖终点时另一端不动、拖回起点得整圆、非等比盒

## 3. `stage-engine`：双击分支与效果

- [x] 3.1 `StageInteractionContext` 增可选 `isGeometryEditable`，缺省即今天的行为
- [x] 3.2 `entity-select-move` 插件加一个分支，文字优先于几何；`StageInteractionEffect` 只增
      `geometry-editing.enter`——退出的三个入口在宿主那边表现为选区或工具变化，再发一条效果
      等于让同一件事有两个说法
- [x] 3.4 **实现时才浮现**：`resize` 插件也要认双击。水平线的包围盒高度接近零，上下两条边缘
      命中区把整条线盖住，双击永远打不到实体本身——而轴对齐的线正是这一刀要救的那一类
- [x] 3.3 插件测试：可几何编辑目标的双击进会话且不开始移动；没注入判定时行为不变

## 4. `stage`：会话、夹点渲染与写入

- [x] 4.1 新增几何编辑 Hook（与文字编辑同构）：进入、退出、当前目标
- [x] 4.2 由文档派生 `StageEditablePath`（切线一律 null、`dots` 为空），经既有覆盖层渲染；
      宿主传入的路径优先，且此时不进会话
- [x] 4.3 变换手柄抑制加一条几何编辑分支
- [x] 4.4 十字光标：会话期间画线，复用步骤 9 的解析函数与那条「画了就收走系统光标」的判定
- [x] 4.5 `useStageDrafting` 导出落点解算，供夹点拖动复用——分叉的症状是「画线时吸端点、
      拖顶点时不吸」；场景索引改由宿主注入并两处共用，因为排除表与解算互为前提
- [x] 4.10 正在编辑的 Entity 从捕捉里排除：它自己的端点就在指针底下，夹点会被吸回原处。
      悬停标记与落点解算读同一份排除表
- [x] 4.6 夹点拖动：移动阶段只更新本地预览，结束阶段派发一条 `entity.curve.set`；越界不钳制
- [x] 4.7 曲线绘制手势结束后进入会话；绘图**命令**产出的对象不进，在实现处写明 `Escape`
      歧义那条理由
- [x] 4.8 退出入口：`Escape`、空白按下、换工具、选中别的 Entity
- [x] 4.9 组件测试：进入与退出、手柄抑制、宿主路径优先、拖夹点派发的命令载荷

## 5. 规范与文档

- [x] 5.1 `stage` 规范：ADD 曲线几何编辑会话；MODIFY 变换手柄抑制、可编辑路径覆盖层、
      路径手势上报（把「事实来源在宿主」收窄到宿主传入的那条）
- [x] 5.2 `stage-engine` 规范：ADD 夹点派生与顶点求解；MODIFY 实体命中的选中与拖动
- [x] 5.3 AGENTS.md：曲线段落补「夹点是可拖的把手，特征点是捕捉目标，两者共用投影但不合并」
      与「几何编辑期间盒手柄让位」
- [x] 5.4 路线图：步骤 10 回填实测；把顶点增删、对齐参考线并轨、矩形升格、旋转曲线重归一化
      四条写进待办与待修缺陷

## 6. 五道门

- [x] 6.1 `bun run lint`
- [x] 6.2 `bun run typecheck`
- [x] 6.3 `bun run test`
- [x] 6.4 `bun run build`
- [x] 6.5 `bun run test:e2e`
