# Tasks

## 1. 判别性用例先验红

- [x] 1.1 e2e：不动任何设置，从左往右拖一个只盖住矩形一半的框，断言**不选中**它。
      **先跑确认红**——今天默认是相交，会选中
- [x] 1.2 同一条：从右往左拖同样的框，断言**选中**它
- [x] 1.3 断言两个方向拖动中的 marquee 填充色**不同**（读 computed style）。今天逐字相同
- [x] 1.4 断言虚实仍然分流：包含实线、相交虚线
- [x] 1.5 断言钉死相交时两个方向都是窗交色、都选中——分色依据是生效判定而不是方向

## 2. 默认值

- [x] 2.1 `DEFAULT_STAGE_MARQUEE_MODE` 改成 `directional`
- [x] 2.2 编辑器 controller 的初值改成 `directional`
- [x] 2.3 Vitest：不传模式时按方向解析

## 3. 分色

- [x] 3.1 新增窗口色/窗交色两个 token（明暗两套），marquee 规则按 `data-marquee-mode` 取用
- [x] 3.2 填充与描边一起换；虚实规则保留不动
- [x] 3.3 选中框、落点反馈等既有蓝不受影响——它们回答的不是「哪一种判定」

## 4. 既有用例逐条确认方向

- [x] 4.1 全量 e2e 跑一遍：**0 条红**——既有 e2e 的框大多完全罩住目标，两种判定给出同一个答案
- [x] 4.2 单元与组件测试：红了 6 条。判据是「这条讲的是判定吗」——讲布尔组合、讲排除
      hidden/locked、讲顺序、讲「空白拖拽起的是框而不是移动」的都不是，逐条**钉死判定模式**；
      只有「未传入模式时回退相交」那条讲的就是默认值本身，改写成「按方向决定」

## 5. 规范与文档

- [x] 5.1 三份规范增量（已写）
- [x] 5.2 AGENTS.md：默认方向决定 + 分色依据是生效判定
- [x] 5.3 路线图：新增一节

## 6. 五道门

- [x] 6.1 `bun run lint`
- [x] 6.2 `bun run typecheck`
- [x] 6.3 `bun run test`
- [x] 6.4 `bun run build`
- [x] 6.5 `bun run test:e2e`
