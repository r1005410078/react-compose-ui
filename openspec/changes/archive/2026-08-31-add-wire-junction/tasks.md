## 1. 物料：节点

- [x] 1.1 在 `packages/materials` 注册 `junction` Preset：整圆 `Curve` + 填实 `Appearance` +
      单端口 `Ports`，`paletteHidden`，直径由线宽推出
- [x] 1.2 单测：与 Wire 共用同一个 Renderer 类型；`Ports.items` 恰好一项且落在盒心；
      直径随线宽变化；默认不在 Palette

## 2. 捕捉：nearest 候选

- [x] 2.1 `feature-points.ts` 为导线产出 `nearest` 候选，复用 `projectComposeCurveToBox` 与
      世界矩阵，不另算一遍；**「是不是导线」由宿主注入谓词**——两端都没接过的导线没有 `Wire`，
      只判它会漏掉最常见的那一类
- [x] 2.2 把 `nearest` 加到 `StageFeatureSnapMode` 与 `MODE_ORDER` 的**末位**
- [x] 2.3 单测（判别性）：线身中间返回最近点；端点与最近点同在容差内且最近点更近时结果是端点；
      不带 `Wire` 的曲线线身不产出候选；在非 100% 缩放下断言

## 3. 接入事务

- [x] 3.1 纯函数：给定被接入导线、落点与来源支路，规划出「建节点 + 断线 + 三次绑定」的命令列表
- [x] 3.2 断线时两段继承原线的呈现与两端绑定，中间拐点按落点所在段分配
- [x] 3.3 落点落在被接入导线首/末顶点时不断线，只改绑
- [x] 3.4 跨父级不接入并在命令行说明；键入坐标产生的落点不接入
- [x] 3.5 接到 `WIRE` / `LINE` 的取点提交路径上，与既有端口绑定走同一个漏斗
- [x] 3.6 单测：一次接入一次撤销；断开两段各自的绑定；交叉不产生任何节点

## 4. 节点清理

- [x] 4.1 支路降到 1 时在同一事务里删掉节点，剩下那条支路变自由端
- [x] 4.2 单测：删到只剩一条时节点消失；剩两条时节点还在；自删与造成它的那一步同一次撤销

## 5. 图面反馈

- [x] 5.1 取点时目标导线整条高亮 + 最近点画沙漏记号（形状与端口记号、捕捉标记都不同）
- [x] 5.2 没有命令在取点时不画；够及范围复用捕捉靶区
- [x] 5.3 组件测试：取点时高亮与记号出现、空闲时消失；节点不选中也画

## 6. 验证

- [x] 6.1 `bun run lint && bun run typecheck && bun run test && bun run build`
- [x] 6.2 `bun run test:e2e`：画一条导线接到另一条上，挪走符号，接头跟着走且不裂开
