# Tasks

## 1. 判别性用例先验红

- [x] 1.1 新增 e2e：**非 100% 缩放**下画两条线，双击其中一条进几何编辑，把它的**中点夹点**
      拖到另一条线的**端点**附近，断言中点**精确落在**那个端点上而不是光标的裸坐标。
      **先跑确认红**——今天直线根本没有中点夹点
- [x] 1.2 同一条继续：断言被拖的那条线长度与方向不变（平移，不是拉伸）
- [x] 1.3 同一条继续：断言进入几何编辑后**选区包围盒消失**、`Escape` 之后恢复。
      **先跑确认红**——今天它一直画着

## 2. `stage-engine`：中点平移夹点

- [x] 2.1 `line` 的夹点集加中点，id 用 `move`
- [x] 2.2 `applyStageCurveGrip` 的 `move` 分支：按中点到落点的位移平移整条线
- [x] 2.3 Vitest：三个夹点、平移后两端位移相同、长度与方向不变
- [x] 2.4 Vitest：`polyline` 与 `arc` 的夹点集一个不变（声明过的例外要有护栏）

## 3. `stage`：选区盒让位

- [x] 3.1 选区层加 `geometryEditing` 条件，与 Resize / 旋转手柄同一处判断
- [x] 3.2 组件测试：会话期间不渲染 `stage-selection-bounds`，退出后回来
- [x] 3.3 确认既有断言 `stage-selection-bounds` 的用例（5 处）都不在几何编辑态，不受影响

## 4. 规范与文档

- [x] 4.1 `stage-engine` 与 `stage` 两份规范增量
- [x] 4.2 AGENTS.md 的几何编辑段落：补「直线的中点夹点是平移」与它的理由（二维特征点捕捉
      vs 对齐吸附是姐妹查询）、「盒手柄与选区盒一并让位」、多段线段中点**留给顶点增删**
- [x] 4.3 路线图「未定的决策 / 顶点增删」补一句：段中点这个位置已被本刀显式保留

## 5. 五道门

- [x] 5.1 `bun run lint`
- [x] 5.2 `bun run typecheck`
- [x] 5.3 `bun run test`
- [x] 5.4 `bun run build`
- [x] 5.5 `bun run test:e2e`

## 6. 过程记录

- 1.1–1.3 先跑确认红：停在「三个夹点」那条断言上（`Received: 2`），此前的准备步骤全绿——
  说明「B 的终点落在网格之外」这条前提当时就成立。
- 判别性另做了一次破坏验证：把落点从端点旁 3 像素改成 30 像素（超出 12 像素捕捉半径），
  中点断言立刻失败（差 154）。因此这条用例验的确实是**捕捉**，不是「拖到哪算哪」。
- 既有用例跟着改的三处，都是本刀直接推翻的旧断言：`geometry-editing` 与
  `stage-interactions` 的夹点数 2 → 3；`stage-interactions` 的「画完曲线后选区边框仍在」
  改成「选区盒一并让位」，并在验命中的那条里先按 `Escape` 退出会话再断言选区。
- 全量 e2e 第一遍出现一次 `curve-vocabulary` 的失败，单跑与随后两次全量都绿；该用例不碰
  选区盒也不碰夹点（`L` 命令产出的对象不进几何编辑），判为既有 flake。

