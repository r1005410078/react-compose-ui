## 1. 解析

- [x] 1.1 纯函数 `parseComposeBulkArray(text, itemSchema)`：切分（换行/制表符/逗号）、去空白、
      丢空项、逐项按 Schema 校验，返回值或「第几项不合法」
- [x] 1.2 反向：把当前数组渲染成一行一项的文本

## 2. UI

- [x] 2.1 数组分组在元素为基本类型时多一个「批量编辑」动作
- [x] 2.2 文本区面（含确认/取消与错误提示），只读时禁用入口；Escape 放弃，Enter 留给换行
- [x] 2.3 文案进 i18n（zh-CN / en-US）
- [x] 2.4 样式

## 3. 用例

- [x] 3.1 单测：三种分隔符解析一致
- [x] 3.2 单测：非法项整体拒绝并报出位置
- [x] 3.3 组件测试：贴进一列数只产生一次受控变更
- [x] 3.4 组件测试：对象数组不出这个入口
- [x] 3.5 组件测试：打开不改再确认是一次无变化提交

## 4. 门禁

- [x] 4.1 `bun run lint` / `typecheck` / `test` / `build` / `test:e2e`
