## 1. 实现

- [x] 1.1 抽出两种形态共用的值行：HEX + 只读 RGBA + 不透明度 + 吸管（+ 透明，当 `allowTransparent`）
- [x] 1.2 删除 `<details>「精确」` 折叠块、它那一份重复的 HEX/不透明度输入，以及把它藏掉的样式规则
- [x] 1.3 色块只在内嵌形态渲染
- [x] 1.4 样式跟上：值行在窄的默认形态里不溢出

## 2. 用例

- [x] 2.1 组件测试：默认形态打开即有一个可编辑的 HEX，无需展开任何东西
- [x] 2.2 组件测试：任一形态下名为 HEX 的输入恰好一个
- [x] 2.3 组件测试：默认形态不渲染面板内色块，内嵌形态渲染
- [x] 2.4 更新受影响的既有用例与视觉黄金文件

## 3. 收尾

- [x] 3.1 skill 里「HEX 藏在折叠的『精确』里」那段绕法删掉
- [x] 3.2 `docs/dashboard-dogfood-issues.md` 的 C-2 标注已修

## 4. 门禁

- [x] 4.1 `bun run lint` / `typecheck` / `test` / `build` / `test:e2e`
