# 任务

## 1. 端口显现

- [x] 1.1 Stage 在取点期间按光标位置求出「够及范围内的带 `Ports` 的 Entity」，复用既有捕捉
      靶区（`snapRadius` 那条），不引第二个距离常数
- [x] 1.2 覆盖层渲染该 Entity 的**全部**端口记号；端口位置走 `getComposeEntityPorts` 并经
      Entity 世界矩阵换算，与捕捉候选读的是同一条链
- [x] 1.3 记号形状与捕捉标记可区分，token 独立（不借 `--compose-stage-snap`）
- [x] 1.4 组件测试：取点期间整组显现、空闲时一个都不画、吸上时两种记号同时在场

## 2. 导线端点状态

- [x] 2.1 端点状态的判定抽成一个入口，Inspector 与图面共用——各判一次必然漂移
- [x] 2.2 选中时画自由 / 已绑定两种记号
- [x] 2.3 失效常驻：不依赖选中集
- [x] 2.4 组件测试：选中显示、取消选择消失、失效不选中也在

## 3. 端到端

- [x] 3.1 e2e：`W` 取点时端口显现，`Escape` 之后消失
- [x] 3.2 e2e：删掉绑定的符号后，失效记号在未选中状态下出现
      （复用 `symbol-wires.spec.ts` 已经建好的那套夹具）

## 4. 文档

- [x] 4.1 `AGENTS.md`：把「端口不画标记，靠捕捉标记在光标靠近时显出来」改写成「不常驻，取点
      时按符号整组显现」，并记下为什么捕捉标记不够（它只标一个点）
- [x] 4.2 `docs/drafting-interaction-sdd.md` §7 拆分表：这一行转 ✅

## 5. 验证

- [x] 5.1 `bun run lint` / `typecheck` / `test` / `build`
- [x] 5.2 `bun run test:e2e`（先 `bun run build`——e2e 跑的是预构建产物）
